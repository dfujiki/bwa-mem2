{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "make",
            "args": ["arch=avx2", "-j", "DEBUG=1"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}